# Docker Compose 环境变量配置示例
# 复制此文件为 .env 并根据需要修改
# 
# 使用方法：
# 1. 复制此文件: cp .env.example .env
# 2. 编辑 .env 文件，修改需要的配置项
# 3. 运行: docker-compose up -d
#
# 注意：.env 文件会被 git 忽略，不会提交到仓库

# ============================================
# 应用端口配置
# ============================================
# 应用对外暴露的端口（宿主机端口:容器端口）
APP_PORT=3000

# ============================================
# 浏览器池配置
# ============================================
# 最大并发连接数
MAX_CONNECTIONS=10
# 连接超时时间（毫秒）
CONNECTION_TIMEOUT=30000
# 连接重试次数
MAX_RETRIES=3
# 重试延迟（毫秒）
RETRY_DELAY=1000

# ============================================
# 搜索服务配置
# ============================================
# 搜索超时时间（毫秒）
SEARCH_TIMEOUT=60000
# 设备类型：mobile 或 desktop（默认 mobile，加速加载）
DEVICE_TYPE=mobile

# ============================================
# 截图配置
# ============================================
# 是否启用错误截图（true/false）
ENABLE_ERROR_SCREENSHOT=true
# 截图保存目录
SCREENSHOT_DIR=./screenshots
# 是否在响应中返回截图 base64（true/false）
SCREENSHOT_IN_RESPONSE=false

# ============================================
# 复制按钮配置
# ============================================
# 是否使用复制按钮方式（true/false）
USE_COPY_BUTTON=true
# 复制按钮等待超时时间（毫秒）
COPY_BUTTON_TIMEOUT=30000
# 点击后等待剪切板更新的时间（毫秒）
CLIPBOARD_WAIT=1000

# ============================================
# Browserless 浏览器服务配置
# ============================================
# 最大并发会话数
MAX_CONCURRENT_SESSIONS=10
# 浏览器连接超时时间（毫秒）
BROWSER_CONNECTION_TIMEOUT=300000